<%= form_for @recipe do |f| %>

    <% if @recipe.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

        <ul>
        <% recipe.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label :title, class:'form-label' %>
      <%= f.text_field :title, class:'form-control' %>
    </div>

    <div class="form-group">
      <%= f.label :description, class:'form-label' %>
      <%= f.text_area :description, class:'form-control' %>
    </div>

  <h5>Choose Products to Associate with this recipe</h5>
   <div class="field">
      <p>
      <%= hidden_field_tag "recipe[product_ids][]", nil %>
        <% Product.all.each do |cat| %>
          <%= check_box_tag "recipe[product_ids][]",  cat.id, @recipe.product_ids.include?(cat.id), id: dom_id(cat) %>
          <%= label_tag dom_id(cat), cat.title %>
          <%= link_to  "edit product", edit_product_path(cat.id) %><br>
        <% end %>
      </p>
    </div>

    <h3>Image</h3>
    <p>
      <%= f.label :recipeimg, class:'form-label' %>
      <%= f.file_field(:recipeimg) %>
    </p>

    <h3>Directions</h3>
    <p>
    <%= f.fields_for :directions, Direction.new do |ff| %>
      <%= ff.label :step %>
      <%= ff.text_field :step %>
    <% end %>
    </p>

    <h3>Ingredients</h3>
    <p>
    <%= f.fields_for :anyingredients, Anyingredient.new do |ff| %>
      <%= ff.label :name %>
      <%= ff.text_field :name %>
      <%= ff.submit 'anoother', :class => 'btn btn-default' %>
    <% end %>
    </p>

    


  <%= f.submit 'Save Recipe', :class => 'btn btn-default' %>
<% end %>


<%= form_for @recipe do |f| %>



   
<div id="recipeDiv">
 
  <h1>Recipe:</h1>
  <!-- One "tab" for each step in the form: -->
  <div class="tab" style="display:block;">Recipe Details
    <p><%= f.label :title %>
      <%= f.text_field :title, oninput:"this.className = ''" %></p>
    <p><%= f.label :description, class:'form-label' %>
      <%= f.text_area :description, class:'form-control', rows:"5" %></p>
  </div>
  <div class="tab">Choose an image:
    <p>
      <%= f.label :recipeimg, class:'form-label' %>
      <%= f.file_field(:recipeimg) %>
    </p>
  </div>
  <div class="tab">choose products:
    <p>
      <%= hidden_field_tag "recipe[product_ids][]", nil %>
        <% Product.all.each do |cat| %>
          <%= check_box_tag "recipe[product_ids][]",  cat.id, @recipe.product_ids.include?(cat.id), id: dom_id(cat) %>
          <%= label_tag dom_id(cat), cat.title %>
          <%= link_to  "edit product", edit_product_path(cat.id) %><br>
        <% end %>
      </p>
  </div>

  <div style="overflow:auto;">
    <div style="float:right;">
      <button type="button" id="prevBtn" >Previous</button>
        <button type="button" id="nextBtn">Next</button>
      <%= f.submit 'Save Recipe', :class => 'btn btn-default',id:"nextBtn" %>
    
    </div>
  </div>
  <!-- Circles which indicates the steps of the form: -->
  <div style="text-align:center;margin-top:40px;">
    <span class="step"></span>
    <span class="step"></span>
    <span class="step"></span>
    
  </div>
<% end %>

</div>
    


 